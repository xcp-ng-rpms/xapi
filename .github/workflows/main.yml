name: spec linter

on:
  push:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - run: sudo apt update && sudo apt install rpmlint 

      - run: rpmlint --checks specfile-warning,superfluous-%clean-section SPECS/xapi.spec
